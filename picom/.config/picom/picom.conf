# Use glx backend for GPU acceleration
backend = "glx";
glx-no-stencil = true;
glx-copy-from-front = false;

# Blur settings
blur-method = "dual_kawase";
blur-strength = 5;
blur-background = true;

# Opacity for that "hacker" look
active-opacity = 0.90;
inactive-opacity = 0.80;
